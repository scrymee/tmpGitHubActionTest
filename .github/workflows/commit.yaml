on:
  push
  workflow-dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          date > genereate.txt
          git add .
          git commit -m 'test'
          git push
        
        
